# cPanel production environment for BMK backend (copy to backend/.env on server)

# --- Database ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=cpanelprefix_bmkuser
DB_PASSWORD=your_strong_db_password
DB_NAME=cpanelprefix_bmk

# --- App ---
NODE_ENV=production
PORT=3001
JWT_SECRET=replace_with_a_long_random_string

# For split front/back only, set your frontend origin (comma-separated if multiple)
# CORS_ORIGIN=https://yourdomain.tld

# --- Email (SMTP) ---
SMTP_HOST=mail.yourdomain.tld
SMTP_PORT=465
SMTP_USER=noreply@yourdomain.tld
SMTP_PASS=your_mailbox_password
SMTP_FROM=BinMasudKuku <noreply@yourdomain.tld>
# Set to 'false' only if your hosting uses custom certs that fail TLS verify
# SMTP_REJECT_UNAUTH=false

# --- Forms / Security ---
# Optional. If not set, form endpoints will allow without CAPTCHA.
# RECAPTCHA_SECRET=your_recaptcha_v2_or_v3_secret

# --- Notes ---
# 1) Do NOT commit real secrets to git.
# 2) This file is read by backend code via dotenv. In cPanel Node App UI,
#    you can either paste these as environment variables or upload as backend/.env.
# 3) Frontend uses REACT_APP_API_URL at build-time; it is not read from this .env.
#    For single-app on cPanel, build the frontend with REACT_APP_API_URL=/api.
