# Copy this file to .env and adjust values for your MySQL setup

# Database connection
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=bmk

# JWT (backend server only; not needed by backfill script)
JWT_SECRET=changeme-super-secret
# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=bmk

# App settings
PORT=5000
NODE_ENV=development
JWT_SECRET=change_me_to_a_long_random_string

# Optional CORS origin (if you want to restrict)
# CORS_ORIGIN=http://localhost:3000
